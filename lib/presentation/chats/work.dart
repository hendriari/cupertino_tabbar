import 'package:flutter/material.dart';

class WorkChats extends StatelessWidget {
  const WorkChats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Work Chats'),
      )
    );
  }
}
